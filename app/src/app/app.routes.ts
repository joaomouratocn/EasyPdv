import { Routes } from '@angular/router';
import { CategoryComponent } from './pages/category-component/category-component';

export const routes: Routes = [{ path: '', component: CategoryComponent }];
