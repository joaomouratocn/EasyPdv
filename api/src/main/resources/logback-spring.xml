<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- Onde os logs serão salvos -->
    <property name="LOG_PATH" value="./logs" />

    <!-- Configuração para o Console (Colorido) -->
    <appender name="Console" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Configuração para o Arquivo -->
    <appender name="RollingFile" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_PATH}/application.log</file>

        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <!-- Cria um novo arquivo por dia: application-2023-10-27.log -->
            <fileNamePattern>${LOG_PATH}/archived/application-%d{yyyy-MM-dd}.log</fileNamePattern>
            <!-- Mantém os logs por 30 dias -->
            <maxHistory>30</maxHistory>
            <!-- Limite total de tamanho da pasta de logs (opcional) -->
            <totalSizeCap>1GB</totalSizeCap>
        </rollingPolicy>

        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Nível de Log para Desenvolvimento -->
    <root level="INFO">
        <appender-ref ref="Console" />
        <appender-ref ref="RollingFile" />
    </root>

    <!-- Log mais detalhado do seu código -->
    <logger name="com.seuprojeto" level="DEBUG" additivity="false">
        <appender-ref ref="Console" />
        <appender-ref ref="RollingFile" />
    </logger>

</configuration>